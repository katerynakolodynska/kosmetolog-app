export const selectContactInfo = (state) => state.contact.data;
export const selectContactLoading = (state) => state.contact.loading;
export const selectContactError = (state) => state.contact.error;
